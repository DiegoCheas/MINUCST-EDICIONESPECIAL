import React from 'react';

// Componente vacío ya que ahora usamos redirección directa a Google Forms
const RegistrationModal: React.FC<{ isOpen: boolean; onClose: () => void }> = () => {
  return null;
};

export default RegistrationModal;